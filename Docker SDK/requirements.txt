# CPU-only builds — keeps the image small and avoids pulling CUDA wheels,
# which aren't needed (and won't fit comfortably) on a free HF Space.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

faiss-cpu==1.8.0
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pillow==10.4.0
numpy==1.26.4
scipy==1.13.1

# Merge this with whatever else main.py/engine.py import that isn't listed
# here — this covers everything engine.py uses, but double-check against
# your existing requirements.txt for anything project-specific.
